name: MusicV1
bot_username: "@music_v1bot"
description: "音乐搜索与下载机器人，支持 /search 搜索，返回编号按钮后点选下载"
version: "1.0"

settings:
  timeout: 20
  retry: 2

commands:
  download:
    steps:
      - action: send_message
        content: "{search_command} {query}"

      - action: wait_response
        expect: inline_buttons
        timeout: 15

      - action: click_button
        strategy: match_text_index
        query: "{query}"

      - action: wait_response
        expect: audio_file
        timeout: 30

      - action: download
        output: "{title}"

  search:
    steps:
      - action: send_message
        content: "{search_command} {query}"

      - action: wait_response
        expect: inline_buttons
        timeout: 15

      - action: respond_list
